<Page x:Class="MethodHelper.Pages.PrefComElement.Basket"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:MethodHelper.Pages.PrefComElement"
      mc:Ignorable="d" 
      d:DesignHeight="860" d:DesignWidth="1540"
      Title="Basket">

    <Border Padding="25">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Preview" Grid.Column="0" FontSize="22"/>

            <StackPanel>
                <TextBlock Text="Preview" FontSize="22"/>
                <DockPanel Width="650">
                    <StackPanel Margin="0 10" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Style="{StaticResource MainBtn}" Background="{StaticResource cyancolor}" x:Name="StoreBtn" Click="StoreBtn_Click">
                            <StackPanel Margin="0 2 0 0" Orientation="Horizontal">
                                <Icon:PackIconMaterial Kind="Store"/>
                                <TextBlock Margin="10 0 0 0" Text="Магазин"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MainBtn}" Margin="10 0 0 0" Background="{StaticResource buttonColor}" x:Name="BasketBtn" Click="BasketBtn_Click">
                            <StackPanel Margin="0 2 0 0" Orientation="Horizontal">
                                <Icon:PackIconMaterial  Kind="Basket"/>
                                <TextBlock Margin="10 0 0 0" Text="Корзина"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MainBtn}" Margin="10 0 0 0" Background="{StaticResource buttonColor}" x:Name="OrderBtn">
                            <StackPanel Margin="0 2 0 0" Orientation="Horizontal">
                                <Icon:PackIconMaterial  Kind="TruckDelivery"/>
                                <TextBlock Margin="10 0 0 0" Text="Заказ"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                        <StackPanel x:Name="ProductValue" Orientation="Horizontal">
                            <TextBlock Text="Показано "/>
                            <TextBlock  x:Name="CurrentCard" Text="0"/>
                            <TextBlock Text=" из "/>
                            <TextBlock  x:Name="AllCard" Text="0"/>
                        </StackPanel>

                        <StackPanel x:Name="BasketValue" Visibility="Collapsed" Orientation="Horizontal">
                            <TextBlock Text="Товаров в корзине: "/>
                            <TextBlock  x:Name="BasketCard" Text="0"/>
                        </StackPanel>
                    </StackPanel>

                </DockPanel>
            </StackPanel>

            <StackPanel Grid.Column="0" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                <Border CornerRadius="10" Width="650" Height="720" Background="{StaticResource panelOverColor}" Padding="10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                            <Border Margin="0 0 10 0" BorderThickness="0" Grid.Column="1" Height="32" CornerRadius="10" Width="315" VerticalAlignment="Top" HorizontalAlignment="Left" Background="White">
                                <StackPanel Orientation="Horizontal">
                                    <TextBox Padding="5 0 0 0" TextChanged="SearchMB_TextChanged" x:Name="SearchMB" VerticalAlignment="Center" FontSize="16" Grid.Row="0" BorderThickness="0" Width="275" HorizontalAlignment="Left" Background="Transparent"/>
                                    <Icon:PackIconMaterial Kind="Magnify" Width="40" Height="20" VerticalAlignment="Center" Foreground="{StaticResource graycolor}" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            
                            <ComboBox Width="150" SelectionChanged="Sorting_SelectionChanged" x:Name="Sorting" Margin="0 0 10 0" Style="{StaticResource settingCB}">
                                <ComboBoxItem>
                                    <TextBlock Margin="0 3 0 0" Text="Без сортировки"/>
                                </ComboBoxItem>
                                <ComboBoxItem>
                                    <TextBlock Margin="0 3 0 0" Text="По убыванию"/>
                                </ComboBoxItem>
                                <ComboBoxItem>
                                    <TextBlock Margin="0 3 0 0" Text="По возрастанию"/>
                                </ComboBoxItem>
                            </ComboBox>

                            <ComboBox Width="145" SelectionChanged="Filter_SelectionChanged" x:Name="Filter" Style="{StaticResource settingCB}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="0 3 0 0" Text="{Binding category_name}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                        
                        <ScrollViewer Background="Transparent" Width="630" Height="660" VerticalAlignment="Bottom" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel>
                                <ListView x:Name="Lv_magazine" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="Transparent" BorderThickness="0" Width="620">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                             <Border Padding="5" CornerRadius="10" Width="600" HorizontalAlignment="Left" Background="AliceBlue">
                                                 <Grid>
                                                     <Grid.ColumnDefinitions>
                                                         <ColumnDefinition Width="84"/>
                                                         <ColumnDefinition Width="*"/>
                                                         <ColumnDefinition Width="130"/>
                                                     </Grid.ColumnDefinitions>
                                                    <Image Grid.Column="0" Source="{Binding product_picture}" Width="64" Margin="10" Height="64"/>
                                                     <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding title}"  Foreground="{StaticResource darkcolor}"/>
                                                        <ScrollViewer Width="370" Height="60">
                                                            <TextBlock Text="{Binding desk}" TextWrapping="Wrap"  Width="350" FontWeight="Light" Foreground="{StaticResource darkcolor}"/>
                                                        </ScrollViewer> 
                                                        <StackPanel VerticalAlignment="Bottom" Width="370" Orientation="Horizontal">
                                                            <TextBlock Text="В наличии: " FontSize="12" Foreground="{StaticResource graycolor}"/>
                                                            <TextBlock Text="{Binding count}" FontSize="12" Foreground="{StaticResource graycolor}"/>
                                                        </StackPanel>
                                                     </StackPanel>
                                                     <StackPanel Grid.Column="2">
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="{Binding price}" Foreground="{StaticResource darkcolor}" HorizontalAlignment="Center"/>
                                                            <TextBlock Text=" руб." Foreground="{StaticResource darkcolor}" HorizontalAlignment="Center"/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal" Margin="0 10 0 5" Width="100">
                                                            <Button IsEnabled="False" Style="{StaticResource DeCountProduct}" Click="DeCountProduct_Click">
                                                                <Icon:PackIconMaterial Kind="Minus" Width="10" Height="10" Foreground="AliceBlue"/>
                                                            </Button>
                                                            <TextBox PreviewTextInput="Count_PreviewTextInput" Text="1" Foreground="{StaticResource darkcolor}" MaxLength="4" Height="25" FontSize="19" VerticalAlignment="Center" Width="50" BorderThickness="0"/>
                                                            <Button Style="{StaticResource AddCountProduct}" Tag="{Binding article}" Click="AddCountProduct_Click">
                                                                <Icon:PackIconMaterial Kind="Plus" Width="10" Height="10" Foreground="AliceBlue"/>
                                                            </Button>
                                                        </StackPanel>
                                                        <Button Style="{StaticResource MainBtn}" Background="{StaticResource yellowcolor}" Click="AddBasket_Click" Tag="{Binding id}" Foreground="{StaticResource darkcolor}" Width="100" Height="25" VerticalAlignment="Bottom" Content="В корзину"/>
                                                     </StackPanel>
                                                 </Grid>
                                             </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                
                                <ListView x:Name="Lv_Basket" Background="Transparent" BorderThickness="0" Width="620" Visibility="Collapsed" Height="700">
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="5" CornerRadius="10" Width="600" HorizontalAlignment="Left" Background="AliceBlue">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="30"/>
                                                        <ColumnDefinition Width="52"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="130"/>
                                                        <ColumnDefinition Width="30"/>
                                                    </Grid.ColumnDefinitions>
                                                    
                                                    <CheckBox Style="{StaticResource Remember}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="0" Width="20" Height="20"/>
                                                    <Image Grid.Column="1" Source="{Binding product.product_picture}" Width="45" Margin="2.5" Height="45"/>
                                                    <StackPanel Grid.Column="2">
                                                        <TextBlock Text="{Binding product.title}" Width="340" Foreground="{StaticResource darkcolor}"/>
                                                        <TextBlock Height="20"/>
                                                        <StackPanel VerticalAlignment="Bottom" Width="340" Orientation="Horizontal">
                                                            <TextBlock Text="В наличии: " FontSize="12" Foreground="{StaticResource graycolor}"/>
                                                            <TextBlock Text="{Binding product.count}" FontSize="12" Foreground="{StaticResource graycolor}"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="3">
                                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                            <TextBlock Text="{Binding product.price}" Foreground="{StaticResource darkcolor}" HorizontalAlignment="Center"/>
                                                            <TextBlock Text=" руб." Foreground="{StaticResource darkcolor}" HorizontalAlignment="Center"/>
                                                        </StackPanel>
                                                        <StackPanel Orientation="Horizontal" Margin="0 10 0 0" Width="100">
                                                            <Button Style="{StaticResource DeCountProduct}">
                                                                <Icon:PackIconMaterial Kind="Minus" Width="10" Height="10" Foreground="AliceBlue"/>
                                                            </Button>
                                                            <TextBox Text="{Binding count_prod}" Foreground="{StaticResource darkcolor}" MaxLength="4" Height="25" FontSize="19" VerticalAlignment="Center" Width="50" BorderThickness="0"/>
                                                            <Button Style="{StaticResource AddCountProduct}">
                                                                <Icon:PackIconMaterial Kind="Plus" Width="10" Height="10" Foreground="AliceBlue"/>
                                                            </Button>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <Button Grid.Column="4" Style="{StaticResource MainBtn}" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource redcolor}" Width="20" Height="20">
                                                        <Icon:PackIconMaterial Kind="Delete"/>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                                
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!--leftzone-->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <DockPanel Margin="0 0 0 15" >
                        <TextBlock DockPanel.Dock="Left" Text="Описание" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="22"/>
                        <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Visibility="Collapsed" Style="{StaticResource MainBtn}" x:Name="addDesc" Click="addDesc_Click" Background="{StaticResource greencolor}" Width="210" Content="Редактировать описание"/>
                    </DockPanel>
                    <ScrollViewer Height="700"  Width="740">
                        <StackPanel x:Name="s_description">
                            <TextBlock x:Name="description" TextWrapping="Wrap"/>
                            <TextBox Visibility="Collapsed" TextWrapping="Wrap" AcceptsReturn="True" Width="740" Opacity="0.8" Height="700" FontSize="14" x:Name="textbox_desc" Padding="5"/>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
                <Grid Grid.Row="1">
                    <Button Style="{StaticResource MainBtn}" Width="200" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Right" Content="Посмотреть код" x:Name="ShowCode" Click="ShowCode_Click" />
                </Grid>
            </Grid>
            <!--leftzone-->
        </Grid>
    </Border>
</Page>
